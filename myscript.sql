CREATE TABLE BARCODESCANLIB.BARCODES

(
barcode varchar(255),
name varchar(255),
count int,
info varchar(255),
picture varchar(255),
PRIMARY KEY (barcode)

);

CREATE TABLE BARCODESCANLIB.BARCODESOFFL

(
barcode varchar(255),
name varchar(255),
count int,
info varchar(255),
picture varchar(255),
PRIMARY KEY (barcode)


);